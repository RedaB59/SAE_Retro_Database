\documentclass[12pt,a4paper]{article}

\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage[french]{babel}
\usepackage[top=2.5cm, bottom=2.5cm, left=2.8cm, right=2.8cm]{geometry}
\usepackage{setspace}
\onehalfspacing

\usepackage{lmodern}
\usepackage{microtype}

\usepackage[dvipsnames,table]{xcolor}
\definecolor{dbtgreen}{HTML}{00855A}
\definecolor{codebg}{HTML}{F0F0F0}
\definecolor{headercolor}{HTML}{2C3E50}
\definecolor{accentblue}{HTML}{2980B9}
\definecolor{warncolor}{HTML}{E67E22}

% Couleurs page de garde
\definecolor{bandeau1}{HTML}{C9BD93}
\definecolor{bandeau2}{HTML}{7ED6CB}
\definecolor{bandeau3}{HTML}{E8744A}
\definecolor{bandeau4}{HTML}{6B5D45}
\definecolor{titlecolor}{HTML}{5F5B3C}
\definecolor{namecolor}{HTML}{2EB8AC}

% ── Code ──────────────────────────────────────────────────────────────────────
\usepackage{listings}
\lstdefinelanguage{SQL}{
  keywords={SELECT,FROM,WHERE,JOIN,LEFT,ON,AS,WITH,GROUP,BY,ORDER,HAVING,
            DISTINCT,COUNT,SUM,AVG,MIN,MAX,CASE,WHEN,THEN,ELSE,END,INSERT,
            UPDATE,SET,CREATE,TABLE,AND,OR,NOT,IN,NULL,COALESCE,ROUND,
            EXTRACT,TRUE,FALSE,INTO,VALUES,OVER,ROW_NUMBER,MD5,CONCAT_WS,
            PERCENTILE_CONT,WITHIN,ANALYZE,BETWEEN,ANY,NULLIF},
  keywordstyle=\color{accentblue}\bfseries,
  morestring=[b]', stringstyle=\color{Mahogany},
  morecomment=[l]{--}, commentstyle=\color{gray}\itshape, sensitive=false
}
\lstdefinelanguage{Python}{
  keywords={import,from,def,class,return,if,else,for,try,except,finally,
            with,as,in,not,and,or,True,False,None,print,len,str,int,float},
  keywordstyle=\color{accentblue}\bfseries,
  morestring=[b]', morestring=[b]", stringstyle=\color{Mahogany},
  morecomment=[l]{\#}, commentstyle=\color{gray}\itshape, sensitive=true
}
\lstdefinelanguage{Jinja}{
  keywords={config,ref,source,is_incremental,if,endif,macro,endmacro,
            materialized,unique_key,post_hook,on_schema_change},
  keywordstyle=\color{dbtgreen}\bfseries,
  morestring=[b]', stringstyle=\color{Mahogany},
  morecomment=[l]{--}, commentstyle=\color{gray}\itshape, sensitive=false
}
\lstset{
  basicstyle=\ttfamily\footnotesize,
  backgroundcolor=\color{codebg},
  frame=single, rulecolor=\color{gray!40},
  breaklines=true, showstringspaces=false,
  numbers=left, numberstyle=\tiny\color{gray}, numbersep=5pt,
  tabsize=2, captionpos=b, xleftmargin=1em
}

% ── Tableaux ──────────────────────────────────────────────────────────────────
\usepackage{booktabs}
\usepackage{tabularx}
\usepackage{longtable}

% ── Liens ─────────────────────────────────────────────────────────────────────
\usepackage{hyperref}
\hypersetup{colorlinks=true, linkcolor=headercolor, urlcolor=accentblue,
  pdftitle={Rapport SAE -- Pipeline DVF}, pdfauthor={Belarbi -- Delrive}}

% ── En-têtes ──────────────────────────────────────────────────────────────────
\usepackage{fancyhdr}
\pagestyle{fancy}
\fancyhf{}
\fancyhead[L]{\small\textcolor{gray}{Intégration des données immobilières dans un DataWareHouse}}
\fancyhead[R]{\small\textcolor{gray}{Belarbi \& Delrive}}
\fancyfoot[C]{\thepage}
\renewcommand{\headrulewidth}{0.4pt}

% ── Divers ────────────────────────────────────────────────────────────────────
\usepackage{graphicx}
\usepackage{enumitem}
\usepackage{tcolorbox}
\tcbuselibrary{skins,breakable}
\usepackage{titlesec}
\usepackage{caption}
\usepackage{float}

% Titres simples sans colorbox
\titleformat{\section}
  {\Large\bfseries\color{headercolor}}{\thesection}{0.8em}{}[\vspace{-0.3em}\rule{\linewidth}{0.6pt}]
\titleformat{\subsection}
  {\large\bfseries\color{headercolor}}{\thesubsection}{0.8em}{}
\titleformat{\subsubsection}
  {\normalsize\bfseries\color{dbtgreen}}{\thesubsubsection}{0.8em}{}

% Chaque \section commence sur une nouvelle page
\let\oldsection\section
\renewcommand{\section}{\clearpage\oldsection}

\newtcolorbox{infobox}[1][]{colback=accentblue!5, colframe=accentblue!50,
  title=#1, fonttitle=\bfseries\small, breakable}
\newtcolorbox{successbox}[1][]{colback=dbtgreen!5, colframe=dbtgreen!50,
  title=#1, fonttitle=\bfseries\small, breakable}
\newtcolorbox{warnbox}[1][]{colback=warncolor!5, colframe=warncolor!50,
  title=#1, fonttitle=\bfseries\small, breakable}

% Commande placeholder image
\newcommand{\imageph}[2]{%
  \begin{tcolorbox}[colback=gray!8, colframe=gray!40,
    title={\small\textit{Image à insérer : #1}}, fonttitle=\bfseries\small]
    \centering\textcolor{gray}{\texttt{#2}}\\[0.3em]
    \textcolor{gray!60}{\small (exporter depuis l'outil de modélisation / capture d'écran)}
  \end{tcolorbox}
}

\newcommand{\dbt}{dbt}
\newcommand{\sql}[1]{\texttt{\small #1}}

% ══════════════════════════════════════════════════════════════════════════════
\begin{document}

\begin{titlepage}
\noindent
{\color{bandeau1}\rule{0.25\linewidth}{4pt}}%
{\color{bandeau2}\rule{0.25\linewidth}{4pt}}%
{\color{bandeau3}\rule{0.25\linewidth}{4pt}}%
{\color{bandeau4}\rule{0.25\linewidth}{4pt}}

\vspace{2em}
\begin{center}
  \includegraphics[width=\linewidth, height=11cm, keepaspectratio]{image.jpg}
\end{center}

\vspace{2.5em}
\noindent
{\Huge\bfseries\color{titlecolor}
  Intégration des données immobilières\\
  dans un DataWareHouse\par}

\vspace{0.6em}
\noindent{\small\color{gray} Rapport de projet -- Juin 2026\\
Année universitaire 2025 -- 2026\par}

\vfill
\noindent
\begin{minipage}[b]{0.6\linewidth}
  {\color{gray} Présenté par :}\\[0.6em]
  {\color{namecolor}\bfseries Reda Belarbi\\[0.2em]
   Delrive}
\end{minipage}%
\hfill
\begin{minipage}[b]{0.35\linewidth}
  \raggedleft
  \includegraphics[width=\linewidth, height=2.5cm, keepaspectratio]{logo.png}
\end{minipage}
\vspace{1em}
\end{titlepage}

\tableofcontents

% ══════════════════════════════════════════════════════════════════════════════
\section{Gestion de projet}
% ══════════════════════════════════════════════════════════════════════════════

\subsection{Organisation et méthodologie}

Le projet a été mené en binôme (Belarbi \& Delrive) sur la période du
22 février au 11 juin 2026, sans répartition stricte des tâches~:
chaque grande étape (modélisation, injection, transformations \dbt{},
dashboard, rédaction) a été réalisée conjointement, avec des points
d'avancement réguliers pour synchroniser le travail.

\subsection{Outils collaboratifs}

\begin{table}[H]
  \centering
  \rowcolors{2}{gray!10}{white}
  \begin{tabularx}{\linewidth}{lX}
    \toprule
    \textbf{Outil} & \textbf{Usage} \\
    \midrule
    \textbf{Git / GitHub}    & Versionning du code (scripts Python, modèles \dbt{},
                                dashboard), historique des commits, sauvegarde collaborative. \\
    \textbf{Trello / Notion} & Suivi des tâches, organisation du backlog
                                et planification de l'avancement. \\
    \textbf{Overleaf}        & Rédaction collaborative du présent rapport en \LaTeX{}. \\
    \bottomrule
  \end{tabularx}
  \caption{Outils collaboratifs utilisés}
\end{table}

\subsection{Planning du projet}

\begin{table}[H]
  \centering
  \rowcolors{2}{gray!10}{white}
  \begin{tabularx}{\linewidth}{lX}
    \toprule
    \textbf{Période} & \textbf{Étape} \\
    \midrule
    22 fév -- 14 mars   & Analyse du sujet, modélisation MCD/MLD, choix des outils \\
    15 mars -- 11 avril & Développement du script Python d'injection (DSA) \\
    12 avril -- 16 mai  & Modèles \dbt{} (staging, intermédiaire, ODS, DWH) et historisation SCD2 \\
    17 mai -- 6 juin    & Conception et développement du dashboard Streamlit \\
    7 -- 11 juin        & Rédaction, relecture et finalisation du rapport \\
    \bottomrule
  \end{tabularx}
  \caption{Planning prévisionnel du projet}
\end{table}

% ══════════════════════════════════════════════════════════════════════════════
\section{Introduction et architecture générale}
% ══════════════════════════════════════════════════════════════════════════════

\subsection{Contexte et objectif}

Ce projet porte sur la construction d'un pipeline de données complet
pour analyser les mutations immobilières françaises à partir des données
publiques DVF (\textit{Demandes de Valeurs Foncières}), publiées
par la DGFiP. L'objectif est de transformer un fichier CSV brut en un
entrepôt analytique avec historisation SCD2 intégrale.

\subsection{Architecture en couches}

\begin{table}[H]
  \centering
  \rowcolors{2}{gray!10}{white}
  \begin{tabularx}{\linewidth}{llXl}
    \toprule
    \textbf{Étape} & \textbf{Schéma PG} & \textbf{Rôle} & \textbf{Outil} \\
    \midrule
    Injection DSA    & \texttt{DSA}           & Normalisation CSV $\to$ 13 tables    & Python \\
    Staging          & \texttt{DSA}           & Typage, renommage, vues              & dbt \\
    Intermédiaire    & \texttt{DSA}           & Jointures, agrégations, hash MD5     & dbt \\
    ODS              & \texttt{Intermediate}  & Stockage incrémental + SCD2          & dbt \\
    DWH Marts        & \texttt{Datawarehouse} & Schéma en étoile + SCD2              & dbt \\
    Dashboard        & ---                    & Visualisation interactive            & Streamlit \\
    \bottomrule
  \end{tabularx}
  \caption{Architecture complète du pipeline}
\end{table}

\subsection{Justification des outils}

\begin{table}[H]
  \centering
  \rowcolors{2}{gray!10}{white}
  \begin{tabularx}{\linewidth}{lX}
    \toprule
    \textbf{Outil} & \textbf{Justification} \\
    \midrule
    \textbf{PostgreSQL} (OVH VPS)
      & Support natif de \sql{PERCENTILE\_CONT}, \sql{width\_bucket},
        \sql{generate\_series}, index partiels, transactions ACID.
        Hébergement partagé sur VPS pour le travail collaboratif. \\
    \dbt{} v1.11
      & Transformation SQL-first avec gestion automatique du DAG,
        matérialisation (vue/table/incrémental), tests de qualité
        intégrés et documentation. Évite les scripts ETL Python complexes. \\
    \textbf{Python + pandas}
      & Lecture du CSV brut, dédoublonnage, génération des clés
        composites, insertion en lot via \sql{execute\_values}. \\
    \textbf{Streamlit + Plotly}
      & Dashboard interactif en pur Python sans frontend JS.
        Plotly supporte choroplèthes, treemaps, timelines Gantt
        et heatmaps. Cache \sql{@st.cache\_data} pour les performances. \\
    \bottomrule
  \end{tabularx}
  \caption{Technologies utilisées et justification}
\end{table}

% ══════════════════════════════════════════════════════════════════════════════
\section{Source de données DVF}
% ══════════════════════════════════════════════════════════════════════════════

\subsection{Format du fichier CSV}

Les données DVF sont publiées en open data sur \texttt{data.gouv.fr}.
Le fichier CSV est dénormalisé : une ligne contient simultanément
les informations de mutation, parcelle, local, lots et adresse.

\begin{table}[H]
  \centering
  \rowcolors{2}{gray!10}{white}
  \begin{tabularx}{\linewidth}{l X X} % Modifié ici : deux colonnes X au lieu de l
    \toprule
    \textbf{Groupe} & \textbf{Colonnes principales} & \textbf{Description} \\
    \midrule
    Transaction  & \texttt{id\_mutation}, \texttt{date\_mutation}, \texttt{valeur\_fonciere}, \texttt{nature\_mutation}
                 & Identifiant, date notariée, prix €, type \\
    \addlinespace % Ajoute un léger espace respirable entre les lignes
    Parcelle     & \texttt{id\_parcelle}, \texttt{code\_nature\_culture}, \texttt{surface\_terrain}
                 & Référence cadastrale, nature sol, surface m² \\
    \addlinespace
    Bâtiment     & \texttt{code\_type\_local}, \texttt{surface\_reelle\_bati}, \texttt{nombre\_pieces\_principales}, \texttt{nombre\_lots}
                 & Type bâtiment, surface, pièces, lots \\
    \addlinespace
    Lots         & \texttt{lot1\_numero} \dots{} \texttt{lot5\_numero}, \texttt{lot1\_surface\_carrez} \dots{} \texttt{lot5\_surface\_carrez}
                 & Jusqu'à 5 lots en colonnes larges (\textit{wide format}) \\
    \addlinespace
    Adresse      & \texttt{adresse\_code\_voie}, \texttt{adresse\_nom\_voie}, \texttt{latitude}, \texttt{longitude}
                 & Code FANTOIR, coordonnées WGS84 \\
    \addlinespace
    Commune      & \texttt{code\_commune}, \texttt{nom\_commune}, \texttt{code\_departement}, \texttt{code\_postal}
                 & Référentiels géographiques INSEE \\
    \bottomrule
  \end{tabularx}
  \caption{Groupes de colonnes du fichier CSV DVF}
\end{table}

\begin{warnbox}[Problèmes du format dénormalisé]
\begin{itemize}[noitemsep]
  \item Redondance : commune et département répétés pour chaque ligne d'une même mutation.
  \item Lots en colonnes larges : 5 lots encodés en \texttt{lot1\_*} à \texttt{lot5\_*} au lieu d'une relation one-to-many.
  \item Clés manquantes : \texttt{id\_adresse} et \texttt{id\_local} n'existent pas dans le CSV -- à générer.
\end{itemize}
\end{warnbox}

% ══════════════════════════════════════════════════════════════════════════════
\section{Injection des données dans la DSA (Python)}
% ══════════════════════════════════════════════════════════════════════════════

Cette section décrit la première étape du pipeline : un script Python qui lit
le fichier CSV brut DVF et le transforme en 13 tables relationnelles
normalisées, insérées dans le schéma DSA de PostgreSQL. Cette étape couvre la
lecture du fichier, le nettoyage des types, le découpage en tables selon le
MLD ci-dessous, la génération des clés manquantes et l'insertion idempotente
en base.

\subsection{Modèle logique de données (MLD)}

Le MLD cible comporte 13 tables relationnelles organisées autour de la
table \texttt{mutations}.

\begin{figure}[H]
  \centering
  \includegraphics[width=\linewidth]{DSA.png}
  \caption{Modèle Logique de Données -- schéma relationnel DSA}
\end{figure}

\subsection{Lecture du CSV et nettoyage}

La première étape consiste à charger l'intégralité du fichier CSV en mémoire
avec pandas, en forçant le typage de toutes les colonnes en chaînes de
caractères. Les valeurs manquantes du CSV sont également converties en
\texttt{None} afin d'être correctement insérées comme \texttt{NULL} en base.

\begin{lstlisting}[language=Python, caption={Lecture du fichier CSV DVF}]
df = pd.read_csv("dvf.csv", dtype=str, sep=",")
df = df.where(pd.notnull(df), None)  # NaN -> None (NULL SQL)
\end{lstlisting}

\begin{infobox}[\texttt{dtype=str} : pourquoi ?]
Lire tout comme chaîne évite l'inférence incorrecte de types (ex.\ code postal
\texttt{"01000"} converti en entier \texttt{1000}). Les casts sont délégués à
la couche Staging dbt.
\end{infobox}

\subsection{Normalisation en 13 tables}

Le DataFrame brut, qui contient une ligne par lot avec de nombreuses colonnes
répétées, est ensuite découpé en 13 sous-tables conformes au MLD. Chaque
sous-table est obtenue par sélection des colonnes utiles, dédoublonnage et
renommage. Lorsqu'une clé primaire n'existe pas dans le CSV d'origine
(par exemple \texttt{id\_adresse} ou \texttt{id\_lot}), elle est générée par
concaténation de colonnes ou par numérotation séquentielle, comme illustré
ci-dessous.

\vspace{2.5em}

\begin{lstlisting}[language=Python, caption={Exemples de normalisation}]
# Departements : dedoublonnage simple
df_departements = df[["code_departement"]].drop_duplicates().dropna()

# nature_mutation : generation d'un id sequentiel
df_nm = df[["nature_mutation"]].drop_duplicates().dropna().reset_index(drop=True)
df_nm["id_nat_mut"] = df_nm.index + 1
nat_mut_map = dict(zip(df_nm["nature_mutation"], df_nm["id_nat_mut"]))

# id_adresse : cle generee par concatenation (reproductible)
df_loc_raw["id_adresse"] = (
    df_loc_raw["code_commune"] + "_" +
    df_loc_raw["adresse_code_voie"] + "_" +
    df_loc_raw["adresse_numero"].fillna("0")
)

# Lots : pivot wide -> tall (lot1_* a lot5_* -> une ligne par lot)
lots_frames = []
for i in range(1, 6):
    tmp = df[["id_mutation","id_parcelle",
              f"lot{i}_numero", f"lot{i}_surface_carrez"]].dropna(subset=[f"lot{i}_numero"])
    tmp = tmp.rename(columns={f"lot{i}_numero": "id_lot",
                               f"lot{i}_surface_carrez": "lot_surface_carrez"})
    lots_frames.append(tmp[["id_lot","id_local","lot_surface_carrez"]])
df_lots = pd.concat(lots_frames).drop_duplicates(subset=["id_lot"])
\end{lstlisting}

\subsection{Insertion en base PostgreSQL}

Une fois les 13 tables construites, elles sont insérées dans PostgreSQL en
respectant l'ordre imposé par les contraintes de clé étrangère (départements
avant communes, mutations avant locals, etc.). Une fonction d'insertion
générique est utilisée pour chaque table, au sein d'une transaction unique.

\vspace{2.5em}


\begin{lstlisting}[language=Python, caption={Insertion batch avec ON CONFLICT DO NOTHING}]
def inserer(cursor, table, df, colonnes):
    donnees = [tuple(r) for r in df[colonnes].itertuples(index=False)]
    execute_values(cursor,
        f"INSERT INTO {table} ({', '.join(colonnes)}) VALUES %s ON CONFLICT DO NOTHING",
        donnees)
    return len(donnees)

# Ordre respectant les cles etrangeres
etapes = [
    ("departements", df_dep, ["code_dep","lib_dep"]),
    ("communes",     df_com, ["code_com","code_postal","lib_com","code_dep"]),
    ("nature_mutation", df_nm, ["id_nat_mut","lib_nat_mut"]),
    # ... 10 autres tables dans l'ordre FK
    ("parcelles_mutations", df_pm, ["id_mutation","id_parcelle"]),
]
conn.autocommit = False
for table, df_t, cols in etapes:
    n = inserer(cur, table, df_t, cols)
    print(f"  {table:<28} -> {n} lignes")
conn.commit()
\end{lstlisting}

\begin{successbox}[Transaction globale + idempotence]
Les 13 insertions sont dans une seule transaction (\texttt{autocommit = False}).
\texttt{ON CONFLICT DO NOTHING} rend le script idempotent : rejouable sans erreur.
\texttt{execute\_values} est ~100x plus rapide qu'un INSERT par ligne.
\end{successbox}

% ══════════════════════════════════════════════════════════════════════════════
\section{Transformations dbt -- Staging et Intermédiaire}
% ══════════════════════════════════════════════════════════════════════════════

Une fois les données chargées dans la DSA, le pipeline dbt prend le relais
pour les transformer progressivement, couche par couche. La couche Staging
effectue un nettoyage léger des tables brutes (typage, renommage), tandis que
la couche Intermédiaire applique la logique métier (déduplication des
versions, agrégations, calcul de hash de contenu) qui servira de base aux
couches suivantes (ODS et DWH).

\subsection{Configuration \texttt{dbt\_project.yml}}

La configuration du projet définit, pour chaque dossier de modèles, le
schéma PostgreSQL cible ainsi que le mode de matérialisation (vue, table ou
incrémental). Cette organisation reflète directement l'architecture en
couches du pipeline.

\vspace{2.5em}


\begin{lstlisting}[language=Jinja]
models:
  Data_Retro_Belarbi_Delrive:
    staging:      {+materialized: view,        +schema: DSA}
    intermediate: {+materialized: table,       +schema: DSA}
    ods:          {+materialized: incremental, +schema: Intermediate}
    marts:
      dimensions: {+materialized: table,       +schema: Datawarehouse}
      facts:      {+materialized: incremental, +schema: Datawarehouse}
\end{lstlisting}

La macro \texttt{generate\_schema\_name} est surchargée pour que dbt écrive
dans \texttt{DSA}, \texttt{Intermediate} et \texttt{Datawarehouse} sans
préfixe automatique.

\subsection{Staging -- typage et renommage (vues)}

Chaque modèle staging effectue uniquement : cast de types, renommage et
horodatage \sql{\_loaded\_at}. Matérialisé en vue pour éviter la
duplication de données.

\vspace{2.5em}

\begin{lstlisting}[language=SQL, caption={stg\_mutations.sql}]
select id_mutation,
       numero_mutation::int       as numero_mutation,
       date_mutation::date        as date_mutation,
       valeur_fonciere::bigint    as valeur_fonciere,
       id_nature_mutation::int    as id_nature_mutation,
       date_chargement,
       current_timestamp          as _loaded_at
from {{ source('DSA', 'mutations') }}
\end{lstlisting}

\subsection{Intermédiaire -- logique métier (tables)}

La couche Intermédiaire matérialise les modèles en tables et y applique la
première vraie logique métier du pipeline : déduplication des versions
successives d'une mutation, agrégation des parcelles associées et calcul
d'un hash de contenu qui servira de référence pour détecter les changements
dans la couche ODS.

\vspace{2.5em}


\begin{lstlisting}[language=SQL, caption={int\_transactions.sql -- déduplication append-only}]
with mutations_brutes as (
    select m.*, pm.id_parcelle,
           row_number() over (
               partition by m.id_mutation
               order by m.date_chargement desc  -- derniere version
           ) as rn
    from {{ ref('stg_mutations') }} m
    join {{ ref('stg_parcelles_mutations') }} pm on pm.id_mutation = m.id_mutation
),
mutations_latest as (select * from mutations_brutes where rn = 1)

select id_mutation, date_mutation, valeur_fonciere,
       count(distinct id_parcelle) as nb_parcelle,
       md5(concat_ws('|', id_mutation, date_mutation::text,
                     valeur_fonciere::text)) as hash_contenu
from mutations_latest
group by id_mutation, date_mutation, id_nature_mutation, valeur_fonciere
\end{lstlisting}

\begin{infobox}[Pattern append-only + déduplication]
La source \texttt{DSA.mutations} accumule les versions sans mise à jour.
\texttt{ROW\_NUMBER() PARTITION BY id\_mutation ORDER BY date\_chargement DESC}
isole la version la plus récente avant agrégation.
\end{infobox}

% ══════════════════════════════════════════════════════════════════════════════
\section{Couche ODS -- Historisation SCD2}
% ══════════════════════════════════════════════════════════════════════════════

L'Operational Data Store (ODS) constitue la mémoire historique du pipeline.
Contrairement aux couches Staging et Intermédiaire, qui ne reflètent que
l'état courant des données, l'ODS applique le pattern SCD Type 2 (Slowly
Changing Dimension) afin de conserver toutes les versions successives de
chaque enregistrement, avec leurs périodes de validité respectives. Cela
permet de répondre à des questions du type \og quelle était la valeur de
cette mutation au 1er janvier 2024 ? \fg{}.

\subsection{Principe du SCD Type 2}

Pour cela, chaque table ODS ajoute aux colonnes métier héritées de la couche
Intermédiaire un ensemble de colonnes techniques permettant de tracer
l'historique des versions :

\begin{table}[H]
  \centering
  \rowcolors{2}{gray!10}{white}
  \begin{tabularx}{\linewidth}{lX}
    \toprule
    \textbf{Colonne} & \textbf{Signification} \\
    \midrule
    \sql{ods\_sk}       & \sql{md5(id || '|' || hash\_contenu)} -- clé de substitution par version \\
    \sql{hash\_contenu} & MD5 des colonnes métier -- détecte les changements \\
    \sql{date\_debut}   & Horodatage d'entrée en vigueur \\
    \sql{date\_fin}     & \sql{'9999-12-31'} si courant, sinon date de fermeture \\
    \sql{est\_courant}  & \sql{true} = version active \\
    \sql{statut\_*}     & \sql{'VALIDE'} ou \sql{'EXPIRE'} \\
    \bottomrule
  \end{tabularx}
  \caption{Colonnes SCD2 dans les tables ODS}
\end{table}

\subsection{Schéma de la couche ODS}

\begin{figure}[H]
  \centering
  \includegraphics[width=\linewidth]{ODS.png}
  \caption{Couche ODS (schéma \texttt{Intermediate}) -- tables et relations SCD2}
\end{figure}

\subsection{Macro \texttt{scd2\_close\_records} et modèle \texttt{ods\_transaction}}

L'historisation SCD2 repose sur deux mécanismes complémentaires exécutés à
chaque run incrémental : l'insertion d'une nouvelle version dès qu'un
changement de hash est détecté, et la fermeture de l'ancienne version
correspondante. Cette fermeture est centralisée dans une macro dbt générique,
réutilisable pour les 8 tables ODS, et déclenchée via un \texttt{post\_hook}.

\vspace{2.5em}


\begin{lstlisting}[language=Jinja, caption={Macro de fermeture SCD2 (post\_hook)}]
{% macro scd2_close_records(target_table, id_col, hash_col, statut_col) %}
UPDATE {{ target_table }} AS cible
SET date_fin = current_timestamp, est_courant = false, {{ statut_col }} = 'EXPIRE'
WHERE cible.est_courant = true
AND EXISTS (
    SELECT 1 FROM {{ target_table }} n
    WHERE n.{{ id_col }} = cible.{{ id_col }}
      AND n.{{ hash_col }} != cible.{{ hash_col }}
      AND n.est_courant = true AND n.date_debut > cible.date_debut
);
{% endmacro %}
\end{lstlisting}

Le modèle \texttt{ods\_transaction} utilise cette macro en \texttt{post\_hook}
et n'insère une nouvelle ligne que si aucune version courante avec le même
hash de contenu n'existe déjà, ce qui évite de dupliquer une mutation
inchangée d'un run à l'autre.

\vspace{2.5em}

\begin{lstlisting}[language=Jinja, caption={ods\_transaction.sql -- modèle incrémental SCD2}]
{{ config(materialized='incremental', unique_key='ods_sk',
  post_hook=["{{ scd2_close_records(this,'id_mutation','hash_contenu',
                                    'statut_transaction') }}",
             "CREATE INDEX IF NOT EXISTS idx_ods_trx_courant ON {{ this }}(est_courant)",
             "ANALYZE {{ this }}"]) }}

select md5(concat_ws('|', s.id_mutation, s.hash_contenu)) as ods_sk,
       gen_random_uuid()        as id_transaction,
       s.id_mutation, s.valeur_fonciere, s.nb_parcelle, s.hash_contenu,
       current_timestamp        as date_debut,
       '9999-12-31'::timestamp  as date_fin,
       true                     as est_courant,
       'VALIDE'                 as statut_transaction
from {{ ref('int_transactions') }} s
{% if is_incremental() %}
where not exists (
    select 1 from {{ this }} t
    where t.hash_contenu = s.hash_contenu and t.est_courant = true
)
{% endif %}
\end{lstlisting}

\begin{successbox}[Fonctionnement en deux temps]
1. INSERT -- nouvelle version insérée avec \sql{est\_courant=true} et \sql{date\_fin='9999-12-31'}.\\
2. UPDATE (post\_hook) -- \texttt{scd2\_close\_records} ferme l'ancienne version
dès que deux versions avec des hash différents coexistent.
\end{successbox}

% ══════════════════════════════════════════════════════════════════════════════
\section{Couche DWH -- Entrepôt analytique}
% ══════════════════════════════════════════════════════════════════════════════

La couche Datawarehouse constitue la finalité analytique du pipeline. Les
données historisées de l'ODS y sont réorganisées selon un schéma en étoile,
optimisé pour les requêtes d'agrégation et de filtrage multidimensionnel
utilisées par le dashboard : une table de faits centrale,
\texttt{fact\_mutation}, reliée à trois dimensions.

\subsection{Schéma en étoile}

\begin{figure}[H]
  \centering
  \includegraphics[width=\linewidth]{DWH.png}
  \caption{Schéma en étoile du \texttt{Datawarehouse} -- \texttt{fact\_mutation} + 3 dimensions}
\end{figure}

\subsection{Modèles du DWH}

\texttt{dim\_temps} génère toutes les dates via \sql{generate\_series}
avec année, mois, trimestre et libellés. Matérialisé en table statique.

\texttt{type\_mutation} et \texttt{localisation} propagent
les colonnes SCD2 depuis l'ODS, permettant d'interroger l'état d'une
dimension à n'importe quelle date (\sql{WHERE est\_courant=true} = état actuel).

\vspace{2.5em}


\begin{lstlisting}[language=Jinja, caption={fact\_mutation.sql -- post\_hook de synchronisation SCD2}]
{{ config(materialized='incremental', unique_key='sk_mutation',
  post_hook=["""
    UPDATE {{ this }} fm
    SET est_courant = ot.est_courant, date_fin = ot.date_fin
    FROM {{ ref('ods_transaction') }} ot
    WHERE fm.id_mutation = ot.id_mutation
      AND fm.sk_mutation = ot.ods_sk
      AND fm.est_courant != ot.est_courant
  """]) }}

select t.ods_sk            as sk_mutation,
       t.id_mutation,
       t.id_nature_mutation::varchar as id_type_mutation,
       t.date_mutation      as id_date,
       t.valeur_fonciere,
       avg(b.surface_batiment)   as avg_surface_bat,
       avg(b.surface_terrain)    as avg_surface_terrain,
       t.nb_parcelle, t.date_debut, t.date_fin, t.est_courant
from {{ ref('ods_transaction') }} t
left join {{ ref('ods_bien_immobilier') }} b on b.id_parcelle=t.id_parcelle
join {{ ref('dim_temps') }} dt on dt.date_=t.date_mutation
join {{ ref('type_mutation') }} tm on tm.id_type_mutation=t.id_nature_mutation::varchar
{% if is_incremental() %}
where not exists (select 1 from {{ this }} fm where fm.sk_mutation=t.ods_sk)
{% endif %}
group by t.ods_sk, t.id_mutation, t.id_nature_mutation,
         t.date_mutation, t.valeur_fonciere, t.nb_parcelle,
         t.date_debut, t.date_fin, t.est_courant
\end{lstlisting}

\begin{table}[H]
  \centering
  \rowcolors{2}{gray!10}{white}
  \begin{tabularx}{\linewidth}{lrl}
    \toprule
    \textbf{Table} & \textbf{Lignes} & \textbf{Remarque} \\
    \midrule
    \texttt{fact\_mutation} & $\sim$7,2 M & Toutes versions SCD2 \\
    \texttt{localisation}   & $\sim$5,3 M & Toutes versions SCD2 \\
    \texttt{type\_mutation} & 8           & Référentiel \\
    \texttt{dim\_temps}     & $\sim$9 000 & 2000 $\to$ aujourd'hui \\
    \bottomrule
  \end{tabularx}
  \caption{Volumes de données dans le Datawarehouse}
\end{table}

% ══════════════════════════════════════════════════════════════════════════════
\section{Tests de qualité des données}
% ══════════════════════════════════════════════════════════════════════════════

Pour garantir la fiabilité du pipeline, et en particulier la cohérence de
l'historisation SCD2, une série de tests dbt est exécutée automatiquement à
chaque run via la commande \texttt{dbt test}. Ces tests combinent des tests
génériques fournis nativement par dbt et des tests personnalisés écrits en
SQL pour vérifier les invariants spécifiques au pattern SCD2 (une seule
version courante par clé naturelle, dates de validité cohérentes, absence de
chevauchement de périodes).

\begin{table}[H]
  \centering
  \rowcolors{2}{gray!10}{white}
  \begin{tabularx}{\linewidth}{lX}
    \toprule
    \textbf{Test} & \textbf{Invariant vérifié} \\
    \midrule
    \texttt{unique} + \texttt{not\_null} & Unicité et non-nullité des surrogate keys (\sql{sk\_*}) \\
    \texttt{accepted\_values}            & \sql{est\_courant} $\in \{true, false\}$ \\
    \texttt{scd2\_courant\_unique}       & Au plus 1 version \sql{est\_courant=true} par clé naturelle \\
    \texttt{scd2\_dates\_coherentes}     & Cohérence \sql{date\_debut/date\_fin/est\_courant} \\
    \texttt{scd2\_pas\_chevauchement}    & Pas de chevauchement de périodes de validité \\
    \bottomrule
  \end{tabularx}
  \caption{Tests de qualité dbt -- génériques et SCD2 personnalisés}
\end{table}

% ══════════════════════════════════════════════════════════════════════════════
\section{Tableau de bord interactif}
% ══════════════════════════════════════════════════════════════════════════════

\subsection{Vue d'ensemble et filtres}

Le dashboard est développé avec Streamlit et Plotly,
connecté directement au schéma \texttt{Datawarehouse} via psycopg2.
Toutes les agrégations sont effectuées côté PostgreSQL -- on ne
rapatrie jamais les 7M de lignes brutes, seulement les résultats agrégés.

\begin{figure}[H]
  \centering
  \includegraphics[width=\linewidth]{overview.png}
  \caption{Vue générale du dashboard -- KPIs et sidebar de filtres}
\end{figure}

La sidebar propose cinq filtres appliqués à toutes les visualisations :
période (date\_input avec bornes BDD),
type de mutation (multiselect),
département (multiselect),
commune (multiselect \textit{dynamique} -- liste rechargée selon
les départements sélectionnés via \texttt{@st.cache\_data(ttl=300)}),
et valeur foncière (slider P0--P99 calculés en SQL).
Six KPIs sont recalculés en temps réel : nb mutations, volume (Md€),
valeur moyenne, médiane, nb communes, prix moyen/m².
Ces filtres permettent de recentrer l'analyse sur un sous-marché précis
(une période donnée, un type de bien, un territoire), et les KPIs offrent
immédiatement une vue chiffrée de l'activité et des prix sur ce périmètre,
avant même de consulter les graphiques détaillés des onglets suivants.

% ── Onglet 1 ──────────────────────────────────────────────────────────────────
\subsection{Onglet 1 -- Temporel}

\begin{figure}[H]
  \centering
  \includegraphics[width=\linewidth]{overview.png}
  \caption{Onglet Temporel -- évolution des mutations dans le temps}
\end{figure}

L'onglet Temporel permet d'analyser l'évolution du marché immobilier avec
une granularité sélectionnable : mois, trimestre ou
année via \sql{DATE\_TRUNC}. Il contient :
\begin{itemize}[noitemsep]
  \item Barres -- nombre de mutations par période
  \item Courbe -- valeur foncière moyenne (€)
  \item Area -- volume total transactionnel (€)
  \item Heatmap mois $\times$ année -- densité des mutations (\sql{px.imshow})
\end{itemize}

Cet onglet sert avant tout à repérer les tendances de fond du marché
(croissance, ralentissement, saisonnalité) et à situer une période donnée
par rapport au reste de l'historique disponible.

% ── Onglet 2 ──────────────────────────────────────────────────────────────────
\subsection{Onglet 2 -- Carte choroplèthe}

\begin{figure}[H]
  \centering
  \includegraphics[width=\linewidth]{carte.png}
  \caption{Onglet Carte -- choroplèthe de France par département}
\end{figure}

La carte de France est générée via \sql{px.choropleth} en chargeant un
GeoJSON public des départements français. Quatre métriques sont
sélectionnables : nombre de mutations, valeur moyenne, volume total,
prix moyen au m². Le survol affiche le détail par département.
Cette carte permet d'identifier en un coup d'œil les disparités
géographiques du marché immobilier français et de repérer les départements
les plus actifs ou les plus chers.

% ── Onglet 3 ──────────────────────────────────────────────────────────────────
\subsection{Onglet 3 -- Géographique}

\begin{figure}[H]
  \centering
  \includegraphics[width=\linewidth]{geo.png}
  \caption{Onglet Géographique -- top départements, communes et treemap}
\end{figure}

Cet onglet propose un classement géographique avec un slider \textit{Top N}
(5 à 30 résultats) et trois métriques (nb mutations, valeur moy., volume) :
\begin{itemize}[noitemsep]
  \item Barres horizontales -- top N départements
  \item Barres horizontales -- top N communes
  \item Treemap département $\to$ commune (couleur = valeur moyenne)
\end{itemize}

Ce classement permet d'identifier rapidement les zones les plus actives ou
les plus valorisées, et de comparer le poids relatif des communes au sein
d'un même département grâce au treemap.

% ── Onglet 4 ──────────────────────────────────────────────────────────────────
\subsection{Onglet 4 -- Prix au m²}

\begin{figure}[H]
  \centering
  \includegraphics[width=\linewidth]{prix.png}
  \caption{Onglet Prix au m² -- analyse du prix/m² par type et département}
\end{figure}

Cet onglet est dédié à l'analyse du prix au m² (\sql{valeur\_fonciere /
NULLIF(avg\_surface\_bat, 0)}) :
\begin{itemize}[noitemsep]
  \item Courbe -- évolution temporelle du prix/m² par type de mutation
  \item Barres -- top 15 départements les plus chers (rouge)
  \item Barres -- top 15 départements les plus abordables (vert)
  \item Scatter -- surface bâtiment vs valeur foncière (échantillon 5 000 mutations)
\end{itemize}

Cet onglet aide à évaluer l'attractivité relative des territoires d'un
point de vue investissement, en comparant le prix au m² entre départements
et en visualisant la relation entre surface et valeur foncière.

% ── Onglet 5 ──────────────────────────────────────────────────────────────────
\subsection{Onglet 5 -- Par type}

\begin{figure}[H]
  \centering
  \includegraphics[width=\linewidth]{type.png}
  \caption{Onglet Par type -- répartition et évolution par nature de mutation}
\end{figure}

L'onglet Par type décompose les données selon la nature de la mutation
(Vente, Adjudication, Échange, etc.) :
\begin{itemize}[noitemsep]
  \item Donut (\sql{hole=0.4}) -- répartition en nombre
  \item Barres -- valeur foncière moyenne par type
  \item Area empilée -- évolution mensuelle du nombre de mutations par type
\end{itemize}

Cette décomposition permet de comprendre la structure du marché (part des
ventes classiques par rapport aux adjudications, échanges, etc.) et de
suivre l'évolution de cette répartition dans le temps.

% ── Onglet 6 ──────────────────────────────────────────────────────────────────
\subsection{Onglet 6 -- Distributions}

\begin{figure}[H]
  \centering
  \includegraphics[width=\linewidth]{distrib.png}
  \caption{Onglet Distributions -- histogramme, quartiles et surfaces}
\end{figure}

Cet onglet analyse la distribution statistique des valeurs :
\begin{itemize}[noitemsep]
  \item Histogramme -- 60 buckets calculés en SQL via \sql{width\_bucket()}
        (aucun rapatriement de données brutes)
  \item Barres groupées -- quartiles Q1/médiane/Q3 par type de mutation
  \item Barres groupées -- surfaces moyennes bâtiment vs terrain par type
  \item Barres + courbe -- mutations et valeur moyenne par nombre de parcelles
\end{itemize}

Ces visualisations permettent de comprendre la dispersion des valeurs et des
surfaces au-delà de la simple moyenne, de repérer d'éventuelles valeurs
atypiques et de comparer les profils de biens selon le nombre de parcelles
concernées par la mutation.

% ══════════════════════════════════════════════════════════════════════════════
\section{Difficultés et solutions}
% ══════════════════════════════════════════════════════════════════════════════

Au cours du développement, plusieurs difficultés techniques ont été
rencontrées et ont nécessité des ajustements du pipeline, parfois en cours
de route. Le tableau ci-dessous résume les principaux problèmes rencontrés,
ainsi que les solutions mises en place pour les résoudre.

\begin{table}[H]
  \centering
  \rowcolors{2}{gray!10}{white}
  \begin{tabularx}{\linewidth}{lX}
    \toprule
    \textbf{Problème} & \textbf{Solution} \\
    \midrule
    Marts s'exécutent avant l'ODS & \sql{source()} $\to$ \sql{ref()} pour créer les dépendances DAG \\
    Disque plein VPS & \texttt{DROP SCHEMA "ODS" CASCADE} + nettoyage \\
    PK sur \texttt{DSA.mutations} (append-only) & \texttt{ALTER TABLE ... DROP CONSTRAINT mutations\_pkey} \\
    \texttt{fact\_mutation} ne capturait qu'une version & Filtre \sql{'VALIDE'} $\to$ \sql{IN ('VALIDE', 'EXPIRE')} \\
    \texttt{Decimal} PostgreSQL incompatible pandas & \texttt{try: pd.to\_numeric(col) except: pass} dans \texttt{run\_query} \\
    Filtre commune non invalidé & Passage de \texttt{dep\_filter} en paramètre de la fonction cachée \\
    Équipe non familière avec \dbt{} & Phase de veille technique (documentation officielle, tutoriels) avant le développement des modèles \\
    Temps d'injection des données très long (CSV de plusieurs millions de lignes) & Insertion par lots via \texttt{execute\_values}, traitement encore perfectible sur le volume complet \\
    \bottomrule
  \end{tabularx}
  \caption{Principales difficultés et solutions}
\end{table}

% ══════════════════════════════════════════════════════════════════════════════
\section{Conclusion}
% ══════════════════════════════════════════════════════════════════════════════

Ce projet implémente un pipeline industriel complet en quatre grandes étapes :

\begin{enumerate}[noitemsep]
  \item Injection Python -- normalisation du CSV dénormalisé en 13 tables,
        génération des clés manquantes, insertion idempotente en lot.
  \item Pipeline dbt 4 couches -- Staging (typage), Intermédiaire (logique
        métier + hash), ODS (SCD2 incrémental), DWH Marts (étoile analytique).
  \item SCD2 généralisé -- 8 tables ODS + table de faits historisée via
        macro \texttt{scd2\_close\_records} et post\_hook de synchronisation.
  \item Dashboard interactif -- 7 onglets, carte choroplèthe, filtre
        commune dynamique, prix/m², toutes les requêtes agrégées côté PostgreSQL.
\end{enumerate}

Perspectives : mise en place d'un orchestrateur tel que Kestra pour
automatiser et planifier l'exécution complète du pipeline (injection,
transformations \dbt{}, tests), optimisation des requêtes (index
supplémentaires, vues matérialisées) afin de réduire les temps de
traitement, enrichissement INSEE (revenus/démographie), géolocalisation
PostGIS.

\end{document}
